type Book {
    id: Int
    title: String
    image: String
    summary: String
}

type Query {
    books(size: String): [Book]
    book(id: Int): Book
}

type Mutation {
    clearBooks(key: String): [Book]
}